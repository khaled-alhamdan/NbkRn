import React from "react";
import {
  View,
  StyleSheet,
} from "react-native";
const Home = () => {
  return (
    <View>
    
    </View>
  );
};

export default Home;



const styles = StyleSheet.create({
  view: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
